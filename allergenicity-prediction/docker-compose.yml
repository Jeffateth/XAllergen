services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    # …    

  frontend:
    build:
      context: ./frontend   # ← drill into the generated folder
      dockerfile: Dockerfile
    ports:
      - "80:80"
    # …
